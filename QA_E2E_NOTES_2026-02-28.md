# E2E QA Notes — 2026-02-28 (live)

## Scope
- Pre-signup marketing pages UX/content/logic
- Signup flow
- Supabase email verification for test account
- Onboarding completion
- CSV import + guest management
- Dashboard flows (excluding deep Vault/Photo Share)
- Builder with provided desktop photos
- Guest registration links / RSVP flows

## Running log

- Pre-signup / signup:
  - Signup form submission appears to stay on page with no explicit success/error feedback in-browser (needs visible state handling).
  - Password fields still trigger browser autocomplete warnings (missing `autocomplete` hints).
- Demo dashboard guards:
  - Login -> demo works.
- Guests & RSVP:
  - Add Guest modal opens with itinerary fallback checkboxes (Ceremony/Reception) when no events exist (good).
  - Add Guest submit in demo appears to no-op (modal remains, no clear success/error toast).
- Itinerary:
  - Add Event form opens correctly.
  - Create Event appears to no-op in demo state (form remains visible, no success/error message, list stays empty).
- Registry:
  - Mixed state issue: empty-state card shown while separate error banner says could not load registry items.
- Builder:
  - Media Library modal opens.
  - File upload via automated upload path reports success but media list remains empty (possible upload integration/refresh bug in demo flow).
- Public site RSVP:
  - RSVP form accepts input but submit gives no visible success or error confirmation and appears unchanged.

