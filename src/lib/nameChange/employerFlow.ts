import { buildNameChangeTargetExecutionSnapshot } from './targetExecution';
import type {
  NameChangeCaseInput,
  NameChangeDocumentInput,
  NameChangeExtractedFieldInput,
  NameChangePlan,
  NameChangeTargetExecutionSnapshot,
} from './types';

export type NameChangeEmployerExecutionSnapshot = NameChangeTargetExecutionSnapshot;

export function buildNameChangeEmployerExecutionSnapshot(
  profile: NameChangeCaseInput,
  documents: NameChangeDocumentInput[],
  extractedFields: NameChangeExtractedFieldInput[],
  plan: NameChangePlan | null = null,
): NameChangeEmployerExecutionSnapshot {
  return buildNameChangeTargetExecutionSnapshot('employer', profile, documents, extractedFields, plan);
}
