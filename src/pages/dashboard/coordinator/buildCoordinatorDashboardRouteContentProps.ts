import type { ComponentProps } from 'react';
import { CoordinatorDashboardRouteContent } from './CoordinatorDashboardRouteContent';

type Props = ComponentProps<typeof CoordinatorDashboardRouteContent>;

export function buildCoordinatorDashboardRouteContentProps(props: Props): Props {
  return props;
}
